# Reference Files

This directory stores source material used while maintaining the structured
rules data in `rules/`.

The files here are reference inputs, not application data. Django imports the
versioned JSON files from `rules/`; it does not read PDFs or other source
documents from this directory at runtime.
